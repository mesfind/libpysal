import { IFileInfo } from './interfaces/files.interface';
import { RemoteBundle } from './http';
/**
 * Splits files in buckets and upload in parallel
 * @param baseURL
 * @param sessionToken
 * @param remoteBundle
 */
export declare function uploadRemoteBundle(baseURL: string, sessionToken: string, bundleId: string, files: IFileInfo[], maxPayload?: number): Promise<boolean>;
export declare function remoteBundleFactory(baseURL: string, sessionToken: string, files: IFileInfo[], removedFiles: string[] | undefined, baseDir: string, existingBundleId: string | null | undefined, maxPayload: number | undefined, source: string): Promise<RemoteBundle | null>;
